
/**
 * config
 */

require.config({
	urlArgs: new Date( ).getTime( ).toString( )
});

/**
 * blindGL
 */

require( [ 'blindgl' ], function( blindGL ) {
	'use strict';

	var engine = new blindGL( );
});
