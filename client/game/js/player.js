
define( [ '../../system/js/player' ], function( aPlayer ) {
	/**
	 * Class: Game
	 */

	var Player = aPlayer.extend({
		/**
		 * Method: addAnimations
		 */

		addAnimations: function( ) {
			this._super( );
		}
	});

	return Player;
});
